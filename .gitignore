# Node / Bun
node_modules/
*.log
.env
.env.local

# macOS
.DS_Store

# Generated by eval runs
.eval-runs/
.eval-worktrees/
*.eval.json
*.eval.md

# (audits live under ~/.prompt-eval/audits/, outside the repo — no .gitignore entry needed.
#  This comment is kept so future contributors don't accidentally re-introduce a local audits/ folder.)

# IDE
.vscode/
.idea/
