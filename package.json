{
  "name": "prompt-eval",
  "version": "0.0.1",
  "description": "Self-improvement framework for Claude Code prompts",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build lib/cli.ts --target=bun --outfile=dist/cli.js",
    "prompt-eval": "bun run dist/cli.js"
  },
  "dependencies": {
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.6.0"
  }
}
